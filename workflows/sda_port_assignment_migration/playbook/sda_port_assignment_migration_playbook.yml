---
- name: Migrate SDA port assignments from source to destination devices
  hosts: catalyst_center_hosts
  connection: local
  gather_facts: no

  vars_files:
    - "{{ VARS_FILE_PATH }}"

  vars:
    state: merged
    cleanup_generated_files: true
    generated_config_dir: "/tmp/sda_port_assignment_migration"
    catalyst_center_login: &catalyst_center_login
      dnac_host: "{{ catalyst_center_host | default(dnac_host) }}"
      dnac_username: "{{ catalyst_center_username | default(dnac_username) }}"
      dnac_password: "{{ catalyst_center_password | default(dnac_password) }}"
      dnac_version: "{{ catalyst_center_version | default(dnac_version) }}"
      dnac_port: "{{ catalyst_center_port | default(443) }}"
      dnac_verify: "{{ catalyst_center_verify | default(dnac_verify) }}"
      config_verify: "{{ catalyst_center_config_verify | default(False) }}"
      dnac_debug: "{{ catalyst_center_debug | default(False) }}"
      dnac_log: "{{ catalyst_center_log | default(False) }}"
      dnac_log_level: "{{ catalyst_center_log_level | default('INFO') }}"
      dnac_log_file_path: "{{ catalyst_center_log_file_path | default(omit) }}"
      dnac_log_append: "{{ catalyst_center_log_append | default(False) }}"
      dnac_api_task_timeout: "{{ catalyst_center_api_task_timeout | default(1200) }}"

  tasks:
    - name: Print input file path
      debug:
        msg: "Input file selected: {{ VARS_FILE_PATH }}"

    - name: Validate migration input list
      fail:
        msg: "port_assignment_migration must contain at least one migration entry."
      when: port_assignment_migration is not defined or port_assignment_migration | length == 0

    - name: Ensure generated config directory exists
      ansible.builtin.file:
        path: "{{ generated_config_dir }}"
        state: directory
        mode: "0755"

    - name: Port assignment migration workflow start time
      set_fact:
        long_op_start: "{{ now() }}"

    - name: Process each source to destination migration entry
      ansible.builtin.include_tasks: ./tasks/migrate_single_port_assignment.yml
      loop: "{{ port_assignment_migration }}"
      loop_control:
        loop_var: migration_item
        label: "{{ migration_item.source_device_ip }} -> {{ migration_item.destination_device_ip }} ({{ migration_item.fabric_site }})"

    - name: Print migration workflow output summary
      debug:
        var: port_assignment_migration_results

    - name: Port assignment migration workflow end time
      set_fact:
        long_op_end: "{{ now() }}"

    - name: Print migration workflow execution time
      debug:
        msg: "Start: {{ long_op_start }}, End: {{ long_op_end }}"

  post_tasks:
    - name: Get python path
      ansible.builtin.command: which python
      register: ansible_play_path
      delegate_to: catalyst_center_hosts
      connection: local
      changed_when: false
