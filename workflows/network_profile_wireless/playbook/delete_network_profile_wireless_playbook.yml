---
- name:  Delete assignment of the wireless SSIDs, Enterprise SSIDs, and Guest SSIDs on Cisco Catalyst Center to the wireless NW profiles for sites
  hosts: catalyst_center_hosts
  connection: local
  gather_facts: no
  vars:
    state: deleted
    catalyst_center_login: &catalyst_center_login
      catalystcenter_host: "{{ catalyst_center_host | default(catalystcenter_host) }}"
      catalystcenter_username: "{{ catalyst_center_username | default(catalystcenter_username) }}"
      catalystcenter_password: "{{ catalyst_center_password | default(catalystcenter_password) }}"
      catalystcenter_version: "{{ catalyst_center_version  | default(catalystcenter_version) }}"
      catalystcenter_port: "{{ catalyst_center_port | default(443) }}"
      catalystcenter_verify: "{{ catalyst_center_verify | default(catalystcenter_verify) }}"
      config_verify: "{{ catalyst_center_config_verify | default(False) }}"
      catalystcenter_debug: "{{ catalyst_center_debug | default(False) }}"
      catalystcenter_log: "{{ catalyst_center_log | default(False) }}"
      catalystcenter_log_level: "{{ catalyst_center_log_level | default('INFO') }}"
      catalystcenter_log_file_path: "{{ catalyst_center_log_file_path |  default(omit) }}"
      catalystcenter_log_append: "{{ catalyst_center_log_append |  default(False) }}"
      catalystcenter_api_task_timeout: "{{ catalyst_center_api_task_timeout |  default(1200) }}"
      
  tasks:
    # =========================================================================
    # Input loading: vars file (VARS_FILE_PATH) or inventory variables
    # =========================================================================
    - name: Load input variables from vars file when VARS_FILE_PATH is provided
      ansible.builtin.include_vars:
        file: "{{ VARS_FILE_PATH }}"
      when:
        - VARS_FILE_PATH is defined
        - VARS_FILE_PATH | length > 0

    - name: Print input source
      ansible.builtin.debug:
        msg: >-
          {{
            'Input source: vars file ' ~ VARS_FILE_PATH
            if (VARS_FILE_PATH is defined and VARS_FILE_PATH | length > 0)
            else 'Input source: inventory / host variables (VARS_FILE_PATH not provided)'
          }}

    - name: Validate that wireless_nw_profiles_details is defined
      ansible.builtin.fail:
        msg: >-
          Variable 'wireless_nw_profiles_details' is not defined.
          Provide it via VARS_FILE_PATH or as an inventory/host variable.
      when: wireless_nw_profiles_details is not defined

    - name: set the password file path
      set_fact:
        passwords_file: "{{ passwords_file  | default('../../../ansible_vault_encrypted_inputs/mypasswordfile.yaml') }}"
      when: jinjatemplate is defined and jinjatemplate is true 
    # load password file
    - name: load password file
      include_vars:
        file: "{{ passwords_file }}"
      when: jinjatemplate is defined and jinjatemplate is true and passwords_file is defined
    # Create inputs from the Jinja Template
    - name: Create inputs from the Jinja Template
      template: 
        src: "{{ jinjatemplate_file }}"
        dest: ../tmp/template_generated_file.yaml
      when:  jinjatemplate is defined and jinjatemplate is true and jinjatemplate_file is defined

    - name: Include the variables file {{ VARS_FILE_PATH | default('inventory / host variables') }} for the playbook
      include_vars:
        file: ../tmp/template_generated_file.yaml
      when: jinjatemplate is defined
    - name: Start time for the playbook
      set_fact:
        long_op_start: "{{ now() }}"
    - name: Delete the Wireless SSID, Interfaces, Custom RF Profiles, etc.
      cisco.catalystcenter.network_profile_wireless_workflow_manager:
        <<: *catalyst_center_login
        state: "{{ state }}"
        config: "{{wireless_nw_profiles_details}}"
      register: wireless_nw_profiles_output
    - name: Print the output of the Wireless SSID, Interfaces, Custom RF Profiles, etc.
      debug:
        msg: "{{wireless_nw_profiles_output}}"
    - name: End time for the playbook
      set_fact:
        long_op_end: "{{ now() }}"
    - name: Wireless SSID, Interfaces, Custom RF Profiles, etc. playbook run time
      debug:
        msg: "Playbook run time is run time: {{ long_op_start }}, end: {{ long_op_end }}"
      when: long_op_start is defined and long_op_end is defined
    - name: Remove the template generated file
      file:
        path: ../tmp/template_generated_file.yaml
        state: absent
      when: jinjatemplate is defined and jinjatemplate is true and jinjatemplate_file is defined
      # End of playbook